---
phase: 04-branding-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/legal/privacy/page.tsx
  - app/legal/cookies/page.tsx
  - app/legal/terms/page.tsx
  - app/legal/layout.tsx
  - components/app/app-footer.tsx
  - components/app/app-shell.tsx
  - components/landing/footer.tsx
  - middleware.ts
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "Privacy policy page is accessible at /legal/privacy without authentication"
    - "Cookie policy page is accessible at /legal/cookies without authentication"
    - "Terms of service page is accessible at /legal/terms without authentication"
    - "Legal pages identify a natural person as data controller, not a company"
    - "All legal page content is in Hungarian"
    - "Footer with legal links appears on every app page"
    - "Landing page footer also links to legal pages"
  artifacts:
    - path: "app/legal/privacy/page.tsx"
      provides: "Adatkezelesi Tajekoztato (GDPR Privacy Policy)"
      contains: "Adatkezelo"
    - path: "app/legal/cookies/page.tsx"
      provides: "Cookie Szabalyzat"
      contains: "cookie"
    - path: "app/legal/terms/page.tsx"
      provides: "Altalanos Szerzodesi Feltetelek"
      contains: "ASZF"
    - path: "app/legal/layout.tsx"
      provides: "Shared legal page layout with DOZIS branding"
      contains: "layout"
    - path: "components/app/app-footer.tsx"
      provides: "Footer component with legal links for app pages"
      contains: "legal"
    - path: "middleware.ts"
      provides: "Updated middleware allowing /legal/* routes without auth"
      contains: "legal"
  key_links:
    - from: "components/app/app-shell.tsx"
      to: "components/app/app-footer.tsx"
      via: "import and render in shell"
      pattern: "AppFooter"
    - from: "components/landing/footer.tsx"
      to: "/legal/*"
      via: "Link elements"
      pattern: "legal"
    - from: "middleware.ts"
      to: "/legal/*"
      via: "public route bypass"
      pattern: "legal"
---

<objective>
Create Hungarian GDPR-compliant legal pages (Privacy Policy, Cookie Policy, Terms of Service) with natural person as data controller, add footer links to all app pages, and ensure legal pages are publicly accessible without authentication.

Purpose: Legal compliance is mandatory for launch. Hungarian GDPR law requires these pages to be accessible before any data collection occurs.

Output: Three legal pages at /legal/*, an app footer with legal links, and updated middleware for public access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-branding-ux/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legal pages layout, privacy policy, cookie policy, and terms of service</name>
  <files>
    app/legal/layout.tsx
    app/legal/privacy/page.tsx
    app/legal/cookies/page.tsx
    app/legal/terms/page.tsx
    middleware.ts
  </files>
  <read_first>
    middleware.ts
    app/layout.tsx
    app/globals.css
    .planning/phases/04-branding-ux/04-CONTEXT.md
  </read_first>
  <action>
**Middleware update** — per D-26:
- In `middleware.ts`, add `/legal` to the public routes check: update the condition at the top to also match `pathname.startsWith("/legal")` so legal pages are accessible without authentication. The existing matcher only matches `/app/:path*`, but since legal pages are at `/legal/*` they would already pass through. However, add the explicit check for safety:
  ```
  if (pathname === "/" || pathname.startsWith("/api/") || pathname === "/app/login" || pathname.startsWith("/legal")) {
    return NextResponse.next();
  }
  ```

**Legal layout** (`app/legal/layout.tsx`):
- Create a shared layout for all legal pages
- DOZIS branding: dark navy background (`bg-dozis-navy-deep`), DOZIS. logo link at top (`/`), back navigation
- Content area: `max-w-3xl mx-auto px-4 py-8`
- Typography: headings use `font-heading text-dozis-amber`, body uses `font-body text-zinc-300`
- Footer links to other legal pages at the bottom

**Privacy Policy** (`app/legal/privacy/page.tsx`) — per D-21, D-24, D-27, D-28, D-29, D-30:
- Page title: "Adatkezelesi Tajekoztato"
- Hungarian metadata: `title: "Adatkezelesi Tajekoztato | DOZIS."`
- Content structure (all Hungarian):
  1. **Adatkezelo** (Data Controller): Natural person — use placeholder `[Teljes Nev]`, `[Email cim]`, `[Lakcim]` — the user will fill in their real data. Note clearly in a comment: `{/* TODO: Replace with actual data controller details */}`
  2. **Kezelt adatok** (Data Collected) — per D-27: email cim, vezeteknev, keresztnev, szuletesi ev, lakcim, esemeny latogatasi elozmeny (attendance history)
  3. **Adatkezeles celja** (Purpose) — per D-28: esemeny regisztracio, becsekkolasi azonositas, husegprogram (loyalty tracking)
  4. **Adatkezeles jogalapja** (Legal Basis): GDPR 6(1)(a) hozzajarulas (consent) — user registers voluntarily; GDPR 6(1)(b) szerzodeses jogalap (contract performance) for event registration
  5. **Adatok megorzese** (Retention) — per D-29: amig a felhasznaloi fiok letezik, torles keresre
  6. **Harmadik felek** (Third Parties) — per D-30: Resend (email kuldes), Cloudflare (tarhelyszolgaltatas es adatfeldolgozas)
  7. **Felhasznaloi jogok** (User Rights): GDPR rights — hozzaferes, helyesbites, torles, korlatozas, hordozhatosag, tiltakozas
  8. **Kapcsolat** (Contact): Same as data controller
  9. **Hatalyba lepes** (Effective Date): use current date placeholder
- Style: use `prose` equivalent with custom Tailwind classes for readability. Headings `font-heading text-xl text-dozis-amber uppercase mt-8 mb-3`, paragraphs `text-zinc-300 leading-relaxed mb-4`, lists `list-disc list-inside text-zinc-300 space-y-1`

**Cookie Policy** (`app/legal/cookies/page.tsx`) — per D-22:
- Page title: "Cookie (Suti) Szabalyzat"
- Content (Hungarian):
  1. **Mik azok a sutik?** (What are cookies?) — brief explanation
  2. **Altalunk hasznalt sutik** (Cookies we use): `better-auth.session_token` — munkamenet suti (session cookie), elengedhetetlen a bejelentkezeshez, nem marketing, nem analytics. Lejarat: munkamenet vege.
  3. **Harmadik feles sutik**: Cloudflare biztonsagi cookie-k (`__cf_bm` stb.)
  4. **Sutik kezelese**: Bongeszobeli beallitasok ismertetese
  5. Minimal cookie usage — no cookie banner needed since only essential cookies are used (GDPR recital 30, ePrivacy Directive Article 5(3) exemption for strictly necessary cookies)

**Terms of Service** (`app/legal/terms/page.tsx`) — per D-23:
- Page title: "Altalanos Szerzodesi Feltetelek (ASZF)"
- Content (Hungarian):
  1. **Szolgaltato** (Provider): Same natural person as data controller with same placeholder
  2. **A szolgaltatas leirasa** (Service Description): Ingyenes esemeny regisztracios platform, QR kodos becsekkolasi rendszer, husegprogram (5. alkalom ingyenes)
  3. **Felhasznalasi feltetelek** (Usage Terms): 18 ev feletti hasznalat, valos adatok megadasa szukseges, egy felhasznalo = egy email cim
  4. **Regisztracio es fiok** (Registration): Email OTP alapu azonositas, profil adatok megadasa kotelezo
  5. **Esemenyek es reszvetel** (Events): Regisztracio nem garantalja a bellepest, QR kod szemelyhez kotott, atruhazhatatlan
  6. **Husegprogram** (Loyalty): 5. alkalom ingyenes, a szamlalo nullazodik utana, a szervezo fenntartja a valtoztatas jogat
  7. **Felelosseg korlatozasa** (Liability): Standard clause — platform "as is", no warranty
  8. **Modositas** (Modification): Fenntartjuk a jogot az ASZF modositasara, ertesites email-ben
  9. **Alkalmazando jog** (Governing Law): Magyar jog, Budapesti birosagok
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20 && test -f app/legal/privacy/page.tsx && test -f app/legal/cookies/page.tsx && test -f app/legal/terms/page.tsx && test -f app/legal/layout.tsx && echo "All legal pages exist"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist (layout + 3 pages)
    - Privacy policy mentions "Adatkezelo" as natural person with placeholder
    - Cookie policy lists only essential cookies (session, CF security)
    - Terms mentions loyalty program, QR code rules, Hungarian law
    - All content is in Hungarian
    - Middleware allows /legal/* without auth
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Three legal pages with GDPR-compliant Hungarian content, shared legal layout with DOZIS branding, middleware updated for public access</done>
</task>

<task type="auto">
  <name>Task 2: App footer with legal links + landing page footer update</name>
  <files>
    components/app/app-footer.tsx
    components/app/app-shell.tsx
    components/landing/footer.tsx
  </files>
  <read_first>
    components/app/app-shell.tsx
    components/landing/footer.tsx
    app/legal/layout.tsx
  </read_first>
  <action>
**App Footer** (`components/app/app-footer.tsx`) — per D-25 (new file):
- Create a new footer component for app pages (not the landing page)
- Layout: horizontal links on one line, centered, with dividers
- Links: "Adatkezelesi Tajekoztato" → `/legal/privacy`, "Cookie Szabalyzat" → `/legal/cookies`, "ASZF" → `/legal/terms`
- Style: `border-t border-zinc-800 bg-dozis-navy-deep py-6 px-4 mt-auto`, links `text-xs text-zinc-500 hover:text-zinc-300 transition-colors`, separated by `mx-2 text-zinc-700` dividers
- Below links: `text-[0.65rem] text-zinc-600 mt-2` with "DOZIS. Elektronikus Zenei Esemenyek"
- Use Next.js `Link` component for internal navigation

**AppShell update** (`components/app/app-shell.tsx`) — per D-25:
- Import and render `AppFooter` at the bottom of the shell
- Change the outer div to `min-h-screen bg-dozis-navy-deep flex flex-col`
- Main content area: `flex-1 container mx-auto px-4 py-8`
- Footer renders after main (sticks to bottom via flex layout)

**Landing page footer update** (`components/landing/footer.tsx`) — per D-25:
- Add legal page links to the existing landing footer
- Add a row below the existing content with the three legal links
- Style: same `text-[0.65rem] tracking-[0.15em] uppercase text-white/20` as the existing "DOPAMIN, BUDAPEST" text
- Links: "Adatvedelem" → `/legal/privacy`, "Sutik" → `/legal/cookies`, "ASZF" → `/legal/terms`
- Use `flex items-center gap-3` with small dot separators between links
- Links open in same tab (internal routes, not external)
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20 && grep -n "AppFooter\|app-footer" components/app/app-shell.tsx | head -5 && grep -n "legal" components/landing/footer.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AppFooter component exists with 3 legal links
    - AppShell imports and renders AppFooter
    - AppShell uses flex-col layout so footer sticks to bottom
    - Landing page footer has legal links
    - All legal links point to correct routes (/legal/privacy, /legal/cookies, /legal/terms)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Every app page shows footer with legal links (via AppShell), landing page footer also links to legal pages</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. Visit `/legal/privacy`, `/legal/cookies`, `/legal/terms` without being logged in — pages render
3. App pages show footer with 3 legal links
4. Landing page footer shows legal links
5. Legal content is entirely in Hungarian
6. Privacy policy identifies natural person data controller (not company)
</verification>

<success_criteria>
- Three legal pages exist and render at /legal/* routes
- Legal pages accessible without authentication
- GDPR-compliant privacy policy with natural person data controller
- Cookie policy documents only essential cookies
- Terms of service covers events, QR codes, loyalty program
- All content in Hungarian
- Footer with legal links on every app page and landing page
</success_criteria>

<output>
After completion, create `.planning/phases/04-branding-ux/04-02-SUMMARY.md`
</output>
