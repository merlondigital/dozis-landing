---
phase: 04-branding-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/app/app-header.tsx
  - components/app/app-shell.tsx
  - components/admin/event-list.tsx
  - components/admin/guest-list.tsx
  - components/checkin/qr-scanner.tsx
  - components/checkin/manual-entry.tsx
  - components/loyalty/loyalty-card.tsx
  - components/events/event-card.tsx
  - components/events/event-hero-card.tsx
  - components/events/qr-display.tsx
  - components/events/register-button.tsx
  - components/events/event-detail-actions.tsx
  - app/app/page.tsx
  - app/app/events/page.tsx
  - app/app/events/[id]/page.tsx
  - app/app/admin/page.tsx
  - app/app/admin/layout.tsx
  - app/app/admin/events/page.tsx
  - app/app/admin/events/[id]/scan/scanner-view.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - INFR-05
  - UX-01
  - UX-02

must_haves:
  truths:
    - "All app pages use DOZIS dark navy background with amber/blue accents consistently"
    - "All interactive elements (buttons, links, inputs) have 44px+ touch targets on mobile"
    - "Admin tables scroll horizontally on mobile without breaking layout"
    - "No English UI text visible to users anywhere in the application"
    - "Typography uses Anton for headings and Montserrat for body across all pages"
    - "Event cards stack single-column on mobile, 2-col tablet, 3-col desktop"
  artifacts:
    - path: "components/ui/button.tsx"
      provides: "44px minimum touch target for default and sm sizes"
      contains: "min-h-\\[44px\\]"
    - path: "components/ui/input.tsx"
      provides: "44px minimum height for inputs"
      contains: "min-h-\\[44px\\]"
    - path: "components/app/app-header.tsx"
      provides: "Mobile-friendly navigation header"
      contains: "DOZIS"
    - path: "app/app/admin/page.tsx"
      provides: "Hungarian admin dashboard title"
      contains: "Admin Vezerlopult"
  key_links:
    - from: "app/globals.css"
      to: "all components"
      via: "Tailwind theme tokens (dozis-navy, dozis-amber, font-heading, font-body)"
      pattern: "dozis-"
    - from: "components/ui/button.tsx"
      to: "all button usages"
      via: "CVA variants with 44px touch targets"
      pattern: "min-h-"
---

<objective>
Apply DOZIS visual identity consistently across all app pages, enforce 44px+ mobile touch targets, make admin tables mobile-friendly with horizontal scroll, and replace all English UI text with Hungarian.

Purpose: The app was built functionality-first in Phases 1-3. This plan brings the entire UI to DOZIS brand standards, ensures mobile usability, and completes Hungarian localization.

Output: Every page and component in the app uses consistent DOZIS branding, has proper mobile touch targets, responsive layouts, and Hungarian text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-branding-ux/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOZIS branding + 44px touch targets on base UI components and app shell</name>
  <files>
    components/ui/button.tsx
    components/ui/input.tsx
    components/app/app-header.tsx
    components/app/app-shell.tsx
    app/globals.css
    app/layout.tsx
  </files>
  <read_first>
    components/ui/button.tsx
    components/ui/input.tsx
    components/app/app-header.tsx
    components/app/app-shell.tsx
    app/globals.css
    app/layout.tsx
    components/landing/footer.tsx
  </read_first>
  <action>
**Button component** (`components/ui/button.tsx`) — per D-05, D-06:
- Update CVA size variants: `default` to `h-11 min-h-[44px] px-5 py-2.5`, `sm` to `h-10 min-h-[44px] px-4 text-sm`, `lg` to `h-12 min-h-[48px] px-8`, `icon` to `h-11 w-11 min-h-[44px]`
- Keep existing variant colors (they already use theme tokens correctly)

**Input component** (`components/ui/input.tsx`) — per D-06:
- Change height from `h-9` to `h-11 min-h-[44px]`
- Add `text-base` (prevents iOS zoom on focus for inputs under 16px)

**AppHeader** (`components/app/app-header.tsx`) — per D-04, D-06, D-11:
- Increase header height from `h-16` to `h-16` (keep, already 64px which is fine)
- Make sign-out button and admin link have `min-h-[44px] min-w-[44px]` touch areas using padding
- On mobile (below md), collapse the email display — show only on md+ with `hidden md:inline`
- Add `font-body` to text elements explicitly for clarity
- Keep DOZIS. logo link

**AppShell** (`components/app/app-shell.tsx`) — per D-05:
- Add bottom padding `pb-24` to main for footer clearance (legal footer will be added in Plan 02)

**globals.css** — per D-03, D-05:
- Add a subtle noise/grain texture class `.dozis-grain` using CSS pseudo-element with a data-URI SVG noise pattern for use on page backgrounds (not the landing page grain animation — a lighter static version)
- Add `.dozis-card` utility class: `bg-dozis-navy rounded-xl border border-zinc-800 p-6` for consistent card styling
- Ensure heading styles (h1-h6) use `text-transform: uppercase` and `letter-spacing: 0.05em` (already partially there, add tracking)

**layout.tsx** (`app/layout.tsx`) — per D-16:
- Update metadata title to "DOZIS. | Elektronikus Zenei Esemenyek"
- Update description to "Regisztralj DOZIS. esemenyekre, kapj QR kodot es gyujtsd az ingyenes belepodet!"
- Add `lang="hu"` (already present, verify)
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Button default size is h-11 (44px), sm size is h-10 min-h-[44px]
    - Input height is h-11 min-h-[44px] with text-base
    - AppHeader collapses email on mobile, touch targets are 44px+
    - globals.css has .dozis-grain and heading tracking
    - Root layout metadata is Hungarian
  </acceptance_criteria>
  <done>Base UI components enforce 44px touch targets, DOZIS branding tokens in globals.css, Hungarian metadata in root layout</done>
</task>

<task type="auto">
  <name>Task 2: Hungarian text audit + mobile-responsive polish across all app pages and components</name>
  <files>
    app/app/admin/page.tsx
    app/app/admin/layout.tsx
    app/app/admin/events/page.tsx
    app/app/admin/events/[id]/scan/scanner-view.tsx
    app/app/page.tsx
    app/app/events/page.tsx
    app/app/events/[id]/page.tsx
    components/admin/event-list.tsx
    components/admin/guest-list.tsx
    components/events/event-card.tsx
    components/events/event-hero-card.tsx
    components/events/qr-display.tsx
    components/events/register-button.tsx
    components/events/event-detail-actions.tsx
    components/loyalty/loyalty-card.tsx
    components/checkin/qr-scanner.tsx
    components/checkin/manual-entry.tsx
  </files>
  <read_first>
    app/app/admin/page.tsx
    app/app/admin/layout.tsx
    app/app/admin/events/page.tsx
    app/app/admin/events/[id]/scan/scanner-view.tsx
    app/app/page.tsx
    app/app/events/page.tsx
    app/app/events/[id]/page.tsx
    components/admin/event-list.tsx
    components/admin/guest-list.tsx
    components/events/event-card.tsx
    components/events/event-hero-card.tsx
    components/events/qr-display.tsx
    components/events/register-button.tsx
    components/events/event-detail-actions.tsx
    components/loyalty/loyalty-card.tsx
    components/checkin/qr-scanner.tsx
    components/checkin/manual-entry.tsx
  </read_first>
  <action>
**Hungarian text replacements** — per D-12, D-13, D-14, D-15:

1. `app/app/admin/page.tsx`: Change "Admin Dashboard" heading to "Admin Vezerlopult"
2. `app/app/admin/layout.tsx`: Change "Admin Panel" bar text to "Admin Felulet"
3. Scan ALL files listed for any remaining English text visible to users and replace with Hungarian. Known items:
   - Any "Loading..." text → "Betoltes..."
   - Any "Back" text → "Vissza"
   - Any "Cancel" text → "Megse"
   - Any "Save" text → "Mentes"
   - Any "Error" text → "Hiba"
   - Any "Success" text → "Sikeres"
   - Any "Search..." placeholder → "Kereses..."
   - Any "No results" → "Nincs talalat"

**Mobile-responsive fixes** — per D-06, D-07, D-08, D-09, D-10:

4. `components/admin/event-list.tsx`: Reflow the action buttons — on mobile (below md), stack buttons in 2x2 grid instead of inline row. Use `flex flex-wrap gap-2` on button container. Ensure each button has `min-h-[44px]`.

5. `components/admin/guest-list.tsx`:
   - Guest table already has `overflow-x-auto` (good)
   - Add `min-w-[600px]` to the inner table so it scrolls properly on mobile — per D-10
   - StatCard grid: already `grid-cols-2 sm:grid-cols-4` (good)

6. `app/app/events/page.tsx`: Update event grid to `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` — per D-09

7. `app/app/page.tsx` (dashboard): Dashboard grid already `grid-cols-1 md:grid-cols-2` (good). Add Hungarian accented characters where missing: "Udvozlunk" → "Udvozlunk" (review all for consistency — the app uses non-accented Hungarian which is a deliberate choice per existing pattern, keep consistent).

8. `components/events/qr-display.tsx`: Ensure QR container is full-width on mobile with `w-full max-w-sm mx-auto`. QR SVG should use `size={Math.min(280, typeof window !== 'undefined' ? window.innerWidth - 96 : 280)}` — actually simpler: use responsive CSS. Set the QRCodeSVG wrapper div to `w-full max-w-[280px]` and pass `size={280}` (SVG scales with CSS).

9. `app/app/admin/events/[id]/scan/scanner-view.tsx`: Ensure scanner view uses `min-h-[calc(100dvh-theme(spacing.32))]` for full viewport on mobile — per D-08.

**Card styling consistency** — per D-05:
10. Verify all Card usages use `bg-dozis-navy border-zinc-800` (some may use default Card which inherits from theme — check if theme defaults are correct in globals.css, they should be since --color-card is set).
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -30 && grep -rn "Admin Dashboard\|Admin Panel\|Loading\.\.\.\|No results" app/ components/ --include="*.tsx" | grep -v node_modules | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero English UI text in any component (grep for common English strings returns empty)
    - Admin dashboard says "Admin Vezerlopult", admin bar says "Admin Felulet"
    - Event listing uses 3-column grid on desktop (lg:grid-cols-3)
    - Admin event list buttons wrap properly on mobile
    - Guest list table scrolls horizontally on mobile (min-w on table)
    - Scanner view fills viewport on mobile
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All user-visible text is Hungarian, all pages are mobile-responsive with proper breakpoints and touch targets, admin tables scroll on mobile</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `grep -rn "Admin Dashboard\|Admin Panel" app/ components/ --include="*.tsx"` — empty (no English admin text)
3. Visual check: all buttons visually 44px+ height
4. Visual check: event cards show 1/2/3 columns at mobile/tablet/desktop
5. Visual check: guest list table scrolls horizontally on mobile
</verification>

<success_criteria>
- DOZIS visual identity (dark navy, amber/blue accents, Anton/Montserrat) consistent across every page
- All buttons and inputs have 44px+ touch targets
- All UI text is Hungarian
- Admin tables are mobile-friendly with horizontal scroll
- Event cards use responsive 1/2/3 column grid
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-branding-ux/04-01-SUMMARY.md`
</output>
