---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - wrangler.jsonc
  - tailwind.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/ui/**
  - lib/utils.ts
  - drizzle.config.ts
  - src/db/schema.ts
  - src/db/index.ts
  - migrations/0000_initial.sql
  - .env.example
  - .dev.vars.example
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03

must_haves:
  truths:
    - "Next.js dev server starts and renders the landing page at localhost:3000"
    - "TypeScript compilation passes with zero errors"
    - "Tailwind CSS classes render correctly in the browser"
    - "D1 database schema can be migrated locally via drizzle-kit"
    - "wrangler.jsonc has D1 binding configured"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js + @opennextjs/cloudflare configuration"
      contains: "opennextjs"
    - path: "wrangler.jsonc"
      provides: "Cloudflare Workers config with D1 binding"
      contains: "d1_databases"
    - path: "src/db/schema.ts"
      provides: "Drizzle ORM schema with users table including role field"
      contains: "role"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit config for D1"
      contains: "d1"
    - path: "app/layout.tsx"
      provides: "Root layout with Tailwind, fonts, metadata"
      contains: "Anton"
    - path: "app/page.tsx"
      provides: "Landing page route at /"
      min_lines: 10
  key_links:
    - from: "src/db/schema.ts"
      to: "drizzle.config.ts"
      via: "schema path reference"
      pattern: "schema.*db/schema"
    - from: "wrangler.jsonc"
      to: "src/db/index.ts"
      via: "D1 binding name"
      pattern: "DB"
---

<objective>
Scaffold the Next.js App Router project with @opennextjs/cloudflare adapter, replacing the current Vite SPA setup. Set up Tailwind CSS + shadcn/ui, Drizzle ORM schema with D1, and migrate the existing landing page to the Next.js `/` route.

Purpose: Establish the full-stack foundation that all subsequent auth and feature work builds on. Without this, nothing else can proceed.
Output: Working Next.js dev server with landing page, TypeScript, Tailwind, Drizzle schema, and Cloudflare config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@CLAUDE.md
@src/App.jsx
@src/styles/variables.css
@index.html
@package.json
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js App Router scaffold with Tailwind + shadcn/ui + Cloudflare adapter</name>
  <files>
    package.json, tsconfig.json, next.config.ts, wrangler.jsonc, tailwind.config.ts,
    app/layout.tsx, app/page.tsx, app/globals.css, app/app/layout.tsx, app/app/page.tsx,
    components.json, lib/utils.ts, postcss.config.mjs,
    .env.example, .dev.vars.example, .gitignore
  </files>
  <read_first>
    package.json, vite.config.js, index.html, src/App.jsx, src/styles/variables.css,
    src/components/DozisLogo/DozisLogo.jsx, src/components/Hero/Hero.jsx,
    src/components/Navbar/Navbar.jsx, src/components/SwirlingBg/SwirlingBg.jsx,
    src/components/About/About.jsx, src/components/Events/Events.jsx,
    src/components/DJs/DJs.jsx, src/components/Footer/Footer.jsx,
    src/components/GrainOverlay/GrainOverlay.jsx,
    .gitignore
  </read_first>
  <action>
    **Per D-01, D-02, D-04, D-06, D-07, D-22, D-23, D-24, D-25:**

    1. **Remove Vite, add Next.js stack:**
       - Remove from package.json: `vite`, `@vitejs/plugin-react`, `eslint-plugin-react-refresh`
       - Remove files: `vite.config.js`, `index.html`, `eslint.config.js`
       - Add dependencies:
         ```
         next: "^15.3"
         @opennextjs/cloudflare: "^1"
         wrangler: "latest"
         tailwindcss: "^4"
         @tailwindcss/postcss: "^4"
         typescript: "^5.7"
         @types/node: "^22"
         @types/react: "^19"
         @types/react-dom: "^19"
         clsx: "^2"
         tailwind-merge: "^3"
         class-variance-authority: "^0.7"
         lucide-react: "^0.500"
         ```
       - Update scripts:
         ```json
         "dev": "next dev",
         "build": "next build",
         "start": "next start",
         "lint": "next lint",
         "preview": "wrangler dev",
         "deploy": "npx @opennextjs/cloudflare build && wrangler deploy",
         "db:generate": "drizzle-kit generate",
         "db:migrate:local": "wrangler d1 migrations apply dozis-db --local",
         "db:migrate:remote": "wrangler d1 migrations apply dozis-db --remote",
         "db:studio": "drizzle-kit studio"
         ```

    2. **Create `next.config.ts`:**
       ```typescript
       import type { NextConfig } from "next";

       const nextConfig: NextConfig = {
         // @opennextjs/cloudflare handles the adapter layer
       };

       export default nextConfig;
       ```

    3. **Create `tsconfig.json`:**
       ```json
       {
         "compilerOptions": {
           "target": "ES2017",
           "lib": ["dom", "dom.iterable", "esnext"],
           "allowJs": true,
           "skipLibCheck": true,
           "strict": true,
           "noEmit": true,
           "esModuleInterop": true,
           "module": "esnext",
           "moduleResolution": "bundler",
           "resolveJsonModule": true,
           "isolatedModules": true,
           "jsx": "preserve",
           "incremental": true,
           "plugins": [{ "name": "next" }],
           "paths": { "@/*": ["./*"] }
         },
         "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
         "exclude": ["node_modules"]
       }
       ```

    4. **Create `wrangler.jsonc`** (per D-08):
       ```jsonc
       {
         "name": "dozis",
         "compatibility_date": "2024-12-01",
         "d1_databases": [
           {
             "binding": "DB",
             "database_name": "dozis-db",
             "database_id": "placeholder-will-be-set-after-d1-create"
           }
         ],
         "vars": {
           "ADMIN_EMAILS": "polosai@example.com,petrus@example.com,dave@example.com"
         }
         // Note: RESEND_API_KEY and BETTER_AUTH_SECRET set via `wrangler secret put`
       }
       ```

    5. **Create `postcss.config.mjs`:**
       ```javascript
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```

    6. **Create `tailwind.config.ts`** (per D-23, D-24, D-25):
       ```typescript
       import type { Config } from "tailwindcss";

       const config: Config = {
         darkMode: "class",
         content: [
           "./app/**/*.{ts,tsx}",
           "./components/**/*.{ts,tsx}",
           "./lib/**/*.{ts,tsx}",
         ],
         theme: {
           extend: {
             colors: {
               // DOZIS brand colors from variables.css
               dozis: {
                 black: "#000000",
                 navy: { DEFAULT: "#0d1f3c", deep: "#0a1628" },
                 amber: { DEFAULT: "#d4a017", light: "#e8b84a", dark: "#b8860b" },
                 blue: { DEFAULT: "#1e40af", light: "#3b82f6" },
               },
             },
             fontFamily: {
               heading: ["var(--font-anton)", "sans-serif"],
               body: ["var(--font-montserrat)", "sans-serif"],
             },
           },
         },
         plugins: [],
       };
       export default config;
       ```

    7. **Create `app/globals.css`:**
       ```css
       @import "tailwindcss";

       @layer base {
         :root {
           --background: 222 47% 11%;
           --foreground: 0 0% 98%;
           --card: 222 47% 11%;
           --card-foreground: 0 0% 98%;
           --popover: 222 47% 11%;
           --popover-foreground: 0 0% 98%;
           --primary: 43 79% 46%;
           --primary-foreground: 0 0% 0%;
           --secondary: 217 91% 60%;
           --secondary-foreground: 0 0% 98%;
           --muted: 240 3.7% 15.9%;
           --muted-foreground: 240 5% 64.9%;
           --accent: 43 79% 46%;
           --accent-foreground: 0 0% 0%;
           --destructive: 0 62.8% 30.6%;
           --destructive-foreground: 0 0% 98%;
           --border: 240 3.7% 15.9%;
           --input: 240 3.7% 15.9%;
           --ring: 43 79% 46%;
           --radius: 0.5rem;
         }
       }

       @layer base {
         * {
           @apply border-border;
         }
         body {
           @apply bg-dozis-navy-deep text-foreground font-body antialiased;
         }
       }
       ```

    8. **Create `lib/utils.ts`** (shadcn/ui utility):
       ```typescript
       import { type ClassValue, clsx } from "clsx";
       import { twMerge } from "tailwind-merge";

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs));
       }
       ```

    9. **Create `components.json`** (shadcn/ui config):
       ```json
       {
         "$schema": "https://ui.shadcn.com/schema.json",
         "style": "new-york",
         "rsc": true,
         "tsx": true,
         "tailwind": {
           "config": "tailwind.config.ts",
           "css": "app/globals.css",
           "baseColor": "zinc",
           "cssVariables": true,
           "prefix": ""
         },
         "aliases": {
           "components": "@/components",
           "utils": "@/lib/utils",
           "ui": "@/components/ui",
           "lib": "@/lib",
           "hooks": "@/hooks"
         },
         "iconLibrary": "lucide"
       }
       ```

    10. **Create `app/layout.tsx`** (per D-24):
        ```typescript
        import type { Metadata } from "next";
        import { Anton, Montserrat } from "next/font/google";
        import "./globals.css";

        const anton = Anton({
          variable: "--font-anton",
          subsets: ["latin"],
          weight: "400",
          display: "swap",
        });

        const montserrat = Montserrat({
          variable: "--font-montserrat",
          subsets: ["latin"],
          weight: ["300", "400", "500", "600", "700", "800"],
          display: "swap",
        });

        export const metadata: Metadata = {
          title: "DOZIS.",
          description: "Budapesti elektronikus zenei kollektiva. Negy DJ. Egy kuldetes.",
        };

        export default function RootLayout({
          children,
        }: {
          children: React.ReactNode;
        }) {
          return (
            <html lang="hu" className="dark">
              <body className={`${anton.variable} ${montserrat.variable}`}>
                {children}
              </body>
            </html>
          );
        }
        ```

    11. **Migrate landing page components from JSX/CSS Modules to TSX/Tailwind.**
        Create `app/page.tsx` as the landing page route (per D-06: `/` = landing page).
        Read each component in `src/components/` (DozisLogo, SwirlingBg, Navbar, Hero, About, Events, DJs, Footer, GrainOverlay), and migrate them to `components/landing/` as TSX with Tailwind classes replacing CSS Modules.
        - Keep the `src/assets/` images — move to `public/images/` so they are statically served.
        - Preserve `src/hooks/useScrollReveal.js` → move to `hooks/use-scroll-reveal.ts`.
        - The landing page must look visually identical after migration (same colors, same layout, same fonts).
        - Remove old `src/` directory after migration is complete.
        - Remove old `dist/` directory (Vite build output, no longer needed).

    12. **Create `.env.example`:**
        ```
        # Cloudflare D1 (for drizzle-kit studio/push)
        CLOUDFLARE_ACCOUNT_ID=
        CLOUDFLARE_DATABASE_ID=

        # better-auth
        BETTER_AUTH_SECRET=generate-a-random-32-char-string
        BETTER_AUTH_URL=http://localhost:3000

        # Resend
        RESEND_API_KEY=
        ```

    13. **Create `.dev.vars.example`** (Cloudflare Workers local dev secrets):
        ```
        BETTER_AUTH_SECRET=dev-secret-change-in-production
        RESEND_API_KEY=re_test_xxxxxxxxxxxx
        ```

    14. **Update `.gitignore`** — add: `.next`, `.open-next`, `.wrangler`, `.dev.vars`, `.env.local`, `.env`

    15. **Create `app/app/layout.tsx`** — app section layout shell (per D-06: `/app/*` = authenticated app):
        ```typescript
        export default function AppLayout({
          children,
        }: {
          children: React.ReactNode;
        }) {
          return (
            <div className="min-h-screen bg-dozis-navy-deep">
              {children}
            </div>
          );
        }
        ```

    16. **Create `app/app/page.tsx`** — placeholder dashboard:
        ```typescript
        export default function DashboardPage() {
          return (
            <div className="flex items-center justify-center min-h-screen">
              <h1 className="font-heading text-4xl text-dozis-amber">
                DOZIS. App
              </h1>
              <p className="text-muted-foreground mt-4">Dashboard - coming soon</p>
            </div>
          );
        }
        ```
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npm install && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies
    - package.json does NOT contain "vite" in dependencies or devDependencies
    - next.config.ts exists at project root
    - tsconfig.json exists with "paths": { "@/*": ["./*"] }
    - wrangler.jsonc contains "d1_databases" and "dozis-db"
    - tailwind.config.ts contains "dozis" color definitions with "#d4a017"
    - app/layout.tsx contains "Anton" and "Montserrat" font imports
    - app/page.tsx exists and renders landing page content
    - app/globals.css contains "@import" and CSS variable definitions
    - lib/utils.ts exports "cn" function
    - components.json exists with shadcn/ui config
    - app/app/layout.tsx exists (app section layout)
    - .gitignore contains ".next" and ".wrangler"
    - `npx tsc --noEmit` exits with code 0
    - `vite.config.js` does NOT exist
    - `index.html` does NOT exist at project root
  </acceptance_criteria>
  <done>
    Next.js App Router project with @opennextjs/cloudflare adapter is scaffolded. Tailwind CSS + shadcn/ui are configured with DOZIS theme colors. Landing page is migrated from JSX/CSS Modules to TSX/Tailwind at the `/` route. The app section layout exists at `/app/*`. TypeScript compiles with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drizzle ORM schema + D1 database configuration</name>
  <files>
    drizzle.config.ts, src/db/schema.ts, src/db/index.ts, migrations/0000_initial.sql
  </files>
  <read_first>
    wrangler.jsonc, package.json, tsconfig.json
  </read_first>
  <action>
    **Per D-03, D-19, D-20 (Drizzle + D1 + better-auth tables + admin role):**

    1. **Add Drizzle dependencies to package.json:**
       ```
       drizzle-orm: "^0.45"
       better-auth: "^1.5"
       ```
       devDependencies:
       ```
       drizzle-kit: "latest"
       ```
       Run `npm install`.

    2. **Create `drizzle.config.ts`:**
       ```typescript
       import { defineConfig } from "drizzle-kit";

       export default defineConfig({
         out: "./migrations",
         schema: "./src/db/schema.ts",
         dialect: "sqlite",
       });
       ```

    3. **Create `src/db/schema.ts`** — Drizzle schema matching better-auth required tables plus custom fields:
       ```typescript
       import { sqliteTable, text, integer } from "drizzle-orm/sqlite-core";

       // better-auth required: user table
       // Custom fields: role (per D-19), lastName, firstName, birthYear, address (per D-11)
       export const user = sqliteTable("user", {
         id: text("id").primaryKey(),
         name: text("name").notNull(),
         email: text("email").notNull().unique(),
         emailVerified: integer("email_verified", { mode: "boolean" }).notNull().default(false),
         image: text("image"),
         role: text("role", { enum: ["user", "admin"] }).notNull().default("user"),
         lastName: text("last_name"),
         firstName: text("first_name"),
         birthYear: integer("birth_year"),
         address: text("address"),
         profileCompleted: integer("profile_completed", { mode: "boolean" }).notNull().default(false),
         createdAt: integer("created_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
         updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
       });

       // better-auth required: session table
       export const session = sqliteTable("session", {
         id: text("id").primaryKey(),
         expiresAt: integer("expires_at", { mode: "timestamp" }).notNull(),
         token: text("token").notNull().unique(),
         ipAddress: text("ip_address"),
         userAgent: text("user_agent"),
         userId: text("user_id").notNull().references(() => user.id, { onDelete: "cascade" }),
         createdAt: integer("created_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
         updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
       });

       // better-auth required: account table
       export const account = sqliteTable("account", {
         id: text("id").primaryKey(),
         accountId: text("account_id").notNull(),
         providerId: text("provider_id").notNull(),
         userId: text("user_id").notNull().references(() => user.id, { onDelete: "cascade" }),
         accessToken: text("access_token"),
         refreshToken: text("refresh_token"),
         idToken: text("id_token"),
         accessTokenExpiresAt: integer("access_token_expires_at", { mode: "timestamp" }),
         refreshTokenExpiresAt: integer("refresh_token_expires_at", { mode: "timestamp" }),
         scope: text("scope"),
         password: text("password"),
         createdAt: integer("created_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
         updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().$defaultFn(() => new Date()),
       });

       // better-auth required: verification table (for OTP codes)
       export const verification = sqliteTable("verification", {
         id: text("id").primaryKey(),
         identifier: text("identifier").notNull(),
         value: text("value").notNull(),
         expiresAt: integer("expires_at", { mode: "timestamp" }).notNull(),
         createdAt: integer("created_at", { mode: "timestamp" }).$defaultFn(() => new Date()),
         updatedAt: integer("updated_at", { mode: "timestamp" }).$defaultFn(() => new Date()),
       });
       ```

    4. **Create `src/db/index.ts`** — D1 database connection helper:
       ```typescript
       import { drizzle } from "drizzle-orm/d1";
       import * as schema from "./schema";

       export function getDb(d1: D1Database) {
         return drizzle(d1, { schema });
       }

       export type Database = ReturnType<typeof getDb>;
       ```

    5. **Generate migration:**
       Run `npx drizzle-kit generate` to produce the initial migration SQL in `./migrations/`.

    6. **Verify migration SQL contains** the `user` table with `role` column, `session` table, `account` table, and `verification` table.
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx drizzle-kit generate 2>&1 && ls migrations/*.sql && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts exists and contains `export const user = sqliteTable`
    - src/db/schema.ts contains `role: text("role"` with enum ["user", "admin"]
    - src/db/schema.ts contains `lastName: text("last_name")`
    - src/db/schema.ts contains `firstName: text("first_name")`
    - src/db/schema.ts contains `birthYear: integer("birth_year")`
    - src/db/schema.ts contains `profileCompleted: integer("profile_completed"`
    - src/db/schema.ts contains `export const session = sqliteTable`
    - src/db/schema.ts contains `export const account = sqliteTable`
    - src/db/schema.ts contains `export const verification = sqliteTable`
    - src/db/index.ts exports `getDb` function
    - drizzle.config.ts contains `dialect: "sqlite"`
    - drizzle.config.ts contains `schema: "./src/db/schema.ts"`
    - At least one .sql file exists in `migrations/` directory
    - package.json contains "drizzle-orm" and "better-auth" in dependencies
    - package.json contains "drizzle-kit" in devDependencies
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    Drizzle ORM schema is defined with all better-auth required tables (user, session, account, verification) plus custom DOZIS fields (role, lastName, firstName, birthYear, address, profileCompleted). D1 connection helper is created. Initial migration SQL is generated. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts the Next.js dev server without errors
- Landing page renders at `http://localhost:3000/` with DOZIS content
- `http://localhost:3000/app` renders the placeholder dashboard
- `npx tsc --noEmit` exits with code 0
- `npx drizzle-kit generate` has produced migration files
- No Vite artifacts remain (no `vite.config.js`, no `index.html` at root)
</verification>

<success_criteria>
- Next.js App Router project runs locally with working landing page at `/`
- Tailwind CSS renders with DOZIS theme colors (amber, navy, zinc)
- Anton + Montserrat fonts load via next/font
- Drizzle schema defines user (with role), session, account, verification tables
- D1 binding configured in wrangler.jsonc
- Migration SQL generated and ready to apply
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
