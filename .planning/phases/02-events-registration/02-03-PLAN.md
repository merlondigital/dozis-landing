---
phase: 02-events-registration
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/app/events/page.tsx
  - app/app/events/[id]/page.tsx
  - app/app/events/[id]/qr/page.tsx
  - components/events/event-card.tsx
  - components/events/event-hero-card.tsx
  - components/events/qr-display.tsx
  - components/events/register-button.tsx
  - app/app/page.tsx
autonomous: false
requirements: [EVNT-04, EVNT-05, REGN-01, REGN-02, REGN-03]

must_haves:
  truths:
    - "User sees upcoming events with the next event as a hero card"
    - "User sees event detail page with all info"
    - "User can register for an event with one tap"
    - "User sees QR code full-screen after registration"
    - "QR code has white background and maximum contrast"
    - "User cannot register twice for the same event"
  artifacts:
    - path: "app/app/events/page.tsx"
      provides: "Event listing page with hero card and upcoming events"
      min_lines: 20
    - path: "app/app/events/[id]/page.tsx"
      provides: "Event detail page with registration button or QR code"
      min_lines: 25
    - path: "app/app/events/[id]/qr/page.tsx"
      provides: "Full-screen QR code display page"
      min_lines: 15
    - path: "components/events/qr-display.tsx"
      provides: "QR code rendering with qrcode.react"
      min_lines: 20
    - path: "components/events/register-button.tsx"
      provides: "One-tap registration button calling registerForEvent"
      min_lines: 15
  key_links:
    - from: "components/events/register-button.tsx"
      to: "src/lib/events/actions.ts"
      via: "Calls registerForEvent Server Action"
      pattern: "registerForEvent"
    - from: "components/events/qr-display.tsx"
      to: "qrcode.react"
      via: "QRCodeSVG component rendering the HMAC token"
      pattern: "QRCodeSVG"
    - from: "app/app/events/page.tsx"
      to: "src/lib/events/actions.ts"
      via: "Calls getUpcomingEvents for listing"
      pattern: "getUpcomingEvents"
    - from: "app/app/events/[id]/page.tsx"
      to: "src/lib/events/actions.ts"
      via: "Calls getEventById and getUserRegistration"
      pattern: "(getEventById|getUserRegistration)"
---

<objective>
Build the user-facing event discovery, registration, and QR code display: event listing with hero card, event detail page, one-tap registration, and full-screen QR code. Also update the dashboard to show real upcoming events.

Purpose: This is the user-facing half of Phase 02 — the core value proposition. Users discover events, register with one tap, and get a QR code to show at the door.
Output: Event listing at /app/events, event detail at /app/events/[id], QR display at /app/events/[id]/qr, updated dashboard with next event.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-events-registration/02-CONTEXT.md
@.planning/phases/02-events-registration/02-01-SUMMARY.md

@app/app/page.tsx
@app/app/layout.tsx
@components/ui/card.tsx
@components/ui/button.tsx
@components/app/app-header.tsx
@lib/utils.ts
@src/lib/auth-utils.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/db/schema.ts (added by Plan 01):
```typescript
export const event = sqliteTable("event", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  date: integer("date", { mode: "timestamp" }).notNull(),
  venue: text("venue").notNull(),
  description: text("description"),
  genreTags: text("genre_tags"), // comma-separated string
  imageUrl: text("image_url"),
  createdBy: text("created_by").notNull(),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
  updatedAt: integer("updated_at", { mode: "timestamp" }).notNull(),
});

export const registration = sqliteTable("registration", {
  id: text("id").primaryKey(),
  eventId: text("event_id").notNull(),
  userId: text("user_id").notNull(),
  qrToken: text("qr_token").notNull(),
  status: text("status", { enum: ["registered", "cancelled"] }).notNull(),
  isFree: integer("is_free", { mode: "boolean" }).notNull(),
  checkedInAt: integer("checked_in_at", { mode: "timestamp" }),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});
```

From src/lib/events/actions.ts (created by Plan 01):
```typescript
export async function registerForEvent(eventId: string): Promise<{ success: true; registrationId: string; qrToken: string } | { error: string }>
export async function cancelRegistration(registrationId: string): Promise<{ success: true } | { error: string }>
export async function getUpcomingEvents(): Promise<Event[]>
export async function getEventById(id: string): Promise<Event | undefined>
export async function getUserRegistration(eventId: string, userId: string): Promise<Registration | undefined>
```

From src/lib/events/validation.ts:
```typescript
export const GENRE_OPTIONS: readonly ["UK Garage", "Club Trance", "Tech House", "Deep House", "Afro House", "Bouncy"]
```

From src/lib/auth-utils.ts:
```typescript
export async function getSession(): Promise<{ user: { id: string; name: string; email: string; role?: string } } | null>
```

Existing components: Button, Card, CardHeader, CardTitle, CardContent, CardFooter, Input, Label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event cards, QR display, register button, and event listing page</name>
  <files>components/events/event-card.tsx, components/events/event-hero-card.tsx, components/events/qr-display.tsx, components/events/register-button.tsx, app/app/events/page.tsx</files>
  <read_first>src/lib/events/actions.ts, src/lib/events/validation.ts, components/ui/card.tsx, components/ui/button.tsx, lib/utils.ts, src/lib/auth-utils.ts, app/app/layout.tsx</read_first>
  <action>
First, install qrcode.react (per D-15, CLAUDE.md stack): `npm install qrcode.react`

**1. Create components/events/event-card.tsx** — "use client" component (per D-06):

Props: `{ event: EventData }` where EventData has id, name, date, venue, genreTags, imageUrl.

Render as a dark card (bg-dozis-navy, border-zinc-800, rounded-xl) showing:
- Event name in text-white font-heading text-xl
- Date in Hungarian format using `date-fns` format with `hu` locale: `format(date, "yyyy. MMMM d., EEEE, HH:mm", { locale: hu })` (e.g., "2026. aprilis 15., szerda, 22:00")
- Venue in text-zinc-400
- Genre tags as colored badges (same color mapping as admin form): each tag is a small rounded pill (px-2 py-0.5 rounded-full text-xs font-medium)
  - "UK Garage" -> bg-amber-500/20 text-amber-400
  - "Club Trance" -> bg-blue-500/20 text-blue-400
  - "Tech House" -> bg-purple-500/20 text-purple-400
  - "Deep House" -> bg-cyan-500/20 text-cyan-400
  - "Afro House" -> bg-green-500/20 text-green-400
  - "Bouncy" -> bg-pink-500/20 text-pink-400
- Entire card is clickable — wraps in `<a href={"/app/events/" + event.id}>` (or Next.js Link)

Parse genreTags: `event.genreTags?.split(",").map(t => t.trim()).filter(Boolean) || []`

**2. Create components/events/event-hero-card.tsx** — "use client" component (per D-07):

Same content as event-card but larger: bigger text (text-3xl name), more padding (p-8), prominent date, border-dozis-amber/30 highlight border, optional gradient overlay. This is the "next event" spotlight card at the top of the listing.

Add a "Reszletek" button at the bottom linking to `/app/events/${event.id}`.

**3. Create components/events/qr-display.tsx** — "use client" component (per D-15, D-16, REGN-03):

Props: `{ qrToken: string; eventName: string; eventDate: Date }`

Render:
- White background container (bg-white, rounded-2xl, p-8) for maximum contrast
- Event name in text-black font-heading text-xl above QR
- Event date in text-gray-600 below name
- `<QRCodeSVG value={qrToken} size={280} level="H" />` from qrcode.react — SVG mode, high error correction for reliable scanning
- "Mutasd meg a bejárátnál" instruction below QR in text-gray-500 text-sm (per D-16)
- The whole container centered on screen

Import: `import { QRCodeSVG } from "qrcode.react";`

**4. Create components/events/register-button.tsx** — "use client" component (per D-17, REGN-01):

Props: `{ eventId: string; onRegistered: (qrToken: string) => void }`

Render a prominent amber button: "Regisztracio" (per D-11).
On click: set loading state, call `registerForEvent(eventId)` Server Action.
On success: call `onRegistered(result.qrToken)` — parent will show QR or redirect.
On error: show error message (e.g., "Mar regisztraltal erre az esemenyre" for duplicate).

Loading state: button text changes to "Regisztracio...", disabled.

**5. Create app/app/events/page.tsx** — Server Component (per D-06, D-07, D-08):

- Call `getUpcomingEvents()` from actions to get future events sorted by date ASC
- Also get current session via `getSession()` for passing userId to child components if needed
- Separate events: first event (closest) -> hero card, rest -> regular cards
- Past events section: if needed, could query past events. For now, only show upcoming (past events can be added later — keep simple for v1).

Layout:
- Title: "Esemenyek" in font-heading text-3xl text-white
- If events exist: EventHeroCard for first event, then grid of EventCards for the rest (1 col mobile, 2 col md)
- If no events: Empty state "Jelenleg nincs kozelgo esemeny" per D-09, styled with DOZIS branding

Past events section (per D-08): below upcoming events, show a "Korabbi esemenyek" divider and list past events as smaller cards (dimmed). Query can be a separate function — the executor should check if actions.ts has a suitable query, or add one inline.
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `qrcode.react` is in package.json dependencies
    - components/events/event-card.tsx renders event info with genre tag colored badges
    - components/events/event-hero-card.tsx renders next event as larger spotlight card
    - components/events/qr-display.tsx uses QRCodeSVG from qrcode.react with white bg and "Mutasd meg a bejárátnál" text
    - components/events/register-button.tsx calls registerForEvent Server Action
    - app/app/events/page.tsx is a Server Component that separates hero card from rest
    - Genre tag colors: amber for UK Garage, blue for Club Trance, purple for Tech House, cyan for Deep House, green for Afro House, pink for Bouncy
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Event listing page shows upcoming events with hero card for next event (EVNT-04), event cards have genre tag badges (D-06), QR display component renders HMAC token as SVG with white background (REGN-03, D-16), register button calls Server Action (REGN-01)</done>
</task>

<task type="auto">
  <name>Task 2: Event detail page, full-screen QR page, and updated dashboard</name>
  <files>app/app/events/[id]/page.tsx, app/app/events/[id]/qr/page.tsx, app/app/page.tsx</files>
  <read_first>src/lib/events/actions.ts, src/lib/auth-utils.ts, components/events/qr-display.tsx, components/events/register-button.tsx, components/events/event-card.tsx, app/app/page.tsx, app/app/layout.tsx</read_first>
  <action>
**1. Create app/app/events/[id]/page.tsx** — Server Component (per D-10, D-11, D-12):

- Extract `id` from params (Next.js 15 async params: `const { id } = await params;`)
- Fetch event via `getEventById(id)` — if not found, show "Esemeny nem talalhato" with link back
- Fetch session via `getSession()` — should always exist (layout protects /app/*)
- Fetch user's registration via `getUserRegistration(id, session.user.id)` — may be undefined

Layout (per D-10):
- Back link: "Vissza" to `/app/events`
- Event name: font-heading text-3xl text-white
- Date: Hungarian formatted, text-dozis-amber
- Venue: text-zinc-300 with map pin icon (use Lucide `MapPin` from lucide-react)
- Genre tags: colored badges (same as event-card)
- Description: text-zinc-400, whitespace-pre-wrap
- Optional image: if imageUrl exists, show as rounded image above details

Registration section at bottom:
- **If NOT registered (no registration or status=cancelled):** Show RegisterButton component (per D-11). When registration succeeds, use `useRouter().push("/app/events/${id}/qr?token=${qrToken}")` to go to QR page. Since this is a Server Component, wrap the registration interaction in a Client Component wrapper (e.g., `EventDetailActions` client component that receives eventId and handles the register flow).
- **If REGISTERED:** Show QrDisplay component inline with the registration's qrToken (per D-12). Also show "Teljes kepernyo" button linking to `/app/events/${id}/qr?token=${registration.qrToken}`. Show "Regisztracio lemondasa" text button (calls cancelRegistration, with confirm dialog).

Since page.tsx is a Server Component but needs interactive registration, create a small client wrapper:
- Create the client interaction inline in this file OR as a small `components/events/event-detail-actions.tsx` client component. Keep it simple — the executor can decide.

**2. Create app/app/events/[id]/qr/page.tsx** — Full-screen QR page (per D-16, REGN-03):

- This is the dedicated full-screen QR view
- Read `searchParams.token` for the qrToken (passed via URL from registration or detail page)
- Also read event ID from params and fetch event for name/date display
- If no token in searchParams, fetch registration from DB: `getUserRegistration(eventId, session.user.id)` and use its qrToken
- Render QrDisplay component FULL SCREEN:
  - Background: pure white (bg-white min-h-screen)
  - Centered QR with event info
  - "Vissza" button at top (text-gray-600, not prominent — the focus is the QR)
  - This entire page should look like a digital ticket — clean, no distractions, maximum QR visibility

**3. Update app/app/page.tsx** — Replace placeholder cards with real event data:

- Keep the greeting ("Szia, {firstName}!")
- Fetch upcoming events via `getUpcomingEvents()` — show the next upcoming event as a card with "Kovetkezo esemeny" heading
  - If user is registered for it, show a small QR icon/badge
  - Link to `/app/events/${event.id}`
- Keep "Husegprogram" placeholder card (Phase 3 will implement loyalty)
- Add "Osszes esemeny" link to `/app/events`

Ensure the dashboard feels like a home screen: next event prominent, quick access to events list.
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/app/events/[id]/page.tsx fetches event by ID and shows full detail per D-10
    - If user is registered, QR code is shown instead of register button per D-12
    - If user is not registered, "Regisztracio" button is shown per D-11
    - app/app/events/[id]/qr/page.tsx renders full-screen white QR display
    - QR page has pure white background (bg-white) for maximum contrast per REGN-03
    - app/app/page.tsx shows next upcoming event instead of placeholder
    - Dashboard links to /app/events for full listing
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Event detail page shows full info with register or QR display (EVNT-05, D-10, D-11, D-12), full-screen QR page with white background (REGN-03, D-16), dashboard shows next event instead of placeholder</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete event + registration flow</name>
  <files>n/a — human verification checkpoint</files>
  <action>
Human verification checkpoint for complete Phase 02 flow.

What was built: admin event CRUD (Plan 02) and user event discovery + registration + QR code (Plan 03), all wired to D1 database via Server Actions.

Prerequisites: D1 database with migrations applied (`npm run db:migrate:local`), dev server running (`npm run dev`).

**Admin flow (as admin user):**
1. Visit /app/admin/events — see empty event list
2. Click "Uj esemeny" — fill form with name, future date, "DOPAMIN, Budapest", select genre tags — submit
3. Event appears in admin list with Hungarian date
4. Click "Szerkesztes" on the event — change description — save
5. Verify changes persisted
6. Create a second event for testing

**User flow (as regular user):**
7. Visit /app — see next upcoming event card on dashboard
8. Visit /app/events — see hero card for next event, regular cards for others
9. Click on an event — see detail page with full info and "Regisztracio" button
10. Tap "Regisztracio" — instantly see QR code on the detail page
11. Tap "Teljes kepernyo" — see full-screen white QR page with "Mutasd meg a bejárátnál"
12. Go back to event detail — QR is still shown (registered state)
13. Go to /app/events — register for a different event
14. Try to register again for same event — error message (duplicate prevented)

**Delete flow (as admin):**
15. Visit /app/admin/events — delete an event — confirm — event disappears, registrations gone

Resume signal: Type "approved" or describe issues found.
  </action>
  <verify>Human verification — all 15 steps above pass</verify>
  <done>Admin can create/edit/delete events, user can browse events/register/view QR code, duplicate registration is blocked, full-screen QR display works with white background</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Event listing page at /app/events with hero card
- Event detail page at /app/events/[id] with registration/QR toggle
- Full-screen QR at /app/events/[id]/qr with white background
- Dashboard shows real upcoming event data
- Human verified: admin CRUD and user registration+QR flow work end-to-end
</verification>

<success_criteria>
- User sees upcoming events with next event highlighted as hero card (EVNT-04)
- User sees event detail page with all info (EVNT-05)
- User can register with one tap and sees QR code immediately (REGN-01)
- QR code is HMAC-signed with registrationId:eventId:userId:hmac format (REGN-02)
- QR code displayed full-screen on white background with max contrast (REGN-03)
- Duplicate registration prevented with user-friendly error (REGN-04)
- Dashboard shows real upcoming event data instead of placeholder
- All verified by human testing end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-events-registration/02-03-SUMMARY.md`
</output>
