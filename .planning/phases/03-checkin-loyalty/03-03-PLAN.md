---
phase: 03-checkin-loyalty
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/app/admin/events/[id]/guests/page.tsx
  - components/admin/guest-list.tsx
  - components/admin/event-list.tsx
  - components/loyalty/loyalty-card.tsx
  - app/app/page.tsx
  - app/app/admin/page.tsx
  - app/app/admin/events/page.tsx
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-03, LOYL-01, CHKN-04]

must_haves:
  truths:
    - "Admin sees guest list with name, email, registration date, status, and check-in time"
    - "Admin can search guests by name or email"
    - "Admin can manually check in a guest from the guest list"
    - "Admin sees stats header: total registered, checked in, no-shows, check-in rate"
    - "User sees loyalty progress card showing X/5 toward next free event"
    - "Admin event list shows registration and check-in counts per event"
    - "Admin event cards have quick links: Szkenner, Vendeglista, Szerkesztes"
  artifacts:
    - path: "app/app/admin/events/[id]/guests/page.tsx"
      provides: "Guest list page route"
    - path: "components/admin/guest-list.tsx"
      provides: "Guest table with search and manual check-in"
    - path: "components/loyalty/loyalty-card.tsx"
      provides: "Loyalty progress display (X/5)"
    - path: "components/admin/event-list.tsx"
      provides: "Enhanced event list with counts and quick links"
    - path: "app/app/page.tsx"
      provides: "Dashboard with loyalty card replacing placeholder"
  key_links:
    - from: "components/admin/guest-list.tsx"
      to: "src/lib/checkin/actions.ts"
      via: "manualCheckInByRegistrationId call"
      pattern: "manualCheckInByRegistrationId"
    - from: "app/app/page.tsx"
      to: "src/lib/checkin/queries.ts"
      via: "getUserLoyalty call"
      pattern: "getUserLoyalty"
    - from: "app/app/admin/events/page.tsx"
      to: "src/lib/checkin/queries.ts"
      via: "getEventRegistrationCounts call"
      pattern: "getEventRegistrationCounts"
---

<objective>
Admin guest management page with search and manual check-in, loyalty progress card for user dashboard, and enhanced admin event list with counts and quick links.

Purpose: Completes the admin oversight loop (see who is registered/checked-in per event) and the user loyalty experience (see progress toward free event). Also enhances the admin event list with at-a-glance registration/check-in info.
Output: Guest list page, loyalty card component, enhanced admin event list and dashboards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-checkin-loyalty/03-CONTEXT.md
@.planning/phases/03-checkin-loyalty/03-01-SUMMARY.md

@src/lib/checkin/actions.ts
@src/lib/checkin/queries.ts
@src/lib/auth-utils.ts
@app/app/page.tsx
@app/app/admin/page.tsx
@app/app/admin/events/page.tsx
@components/admin/event-list.tsx

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/checkin/queries.ts (created in Plan 01):
```typescript
type GuestRow = {
  registrationId: string;
  userId: string;
  name: string;
  email: string;
  firstName: string | null;
  lastName: string | null;
  status: string;
  isFree: boolean;
  registeredAt: Date;
  checkedInAt: Date | null;
};

type EventStats = {
  totalRegistered: number;
  checkedIn: number;
  noShows: number;
  checkInRate: number;
};

type UserLoyalty = {
  attendanceCount: number;
  progress: number;    // 0-4
  nextIsFree: boolean; // true when progress >= 4
};

export async function getEventGuests(eventId: string): Promise<GuestRow[]>;
export async function getEventStats(eventId: string): Promise<EventStats>;
export async function getUserLoyalty(userId: string): Promise<UserLoyalty>;
export async function getEventRegistrationCounts(eventIds: string[]): Promise<Map<string, { registered: number, checkedIn: number }>>;
```

From src/lib/checkin/actions.ts (created in Plan 01):
```typescript
export async function manualCheckInByRegistrationId(registrationId: string, eventId: string): Promise<CheckInResult>;
```

From components/admin/event-list.tsx (existing):
```typescript
export interface EventData {
  id: string; name: string; date: Date; venue: string;
  description: string | null; genreTags: string | null;
  imageUrl: string | null; createdBy: string;
  createdAt: Date; updatedAt: Date;
}
interface EventListProps { events: EventData[]; }
```

From app/app/page.tsx (existing):
- Server Component, calls requireProfile(), getUpcomingEvents(), getUserRegistration()
- Has "Husegprogram" placeholder card that needs replacing with loyalty data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest list page with search, stats header, and manual check-in</name>
  <files>app/app/admin/events/[id]/guests/page.tsx, components/admin/guest-list.tsx</files>
  <read_first>src/lib/checkin/queries.ts, src/lib/checkin/actions.ts, app/app/admin/events/[id]/edit/page.tsx, components/admin/event-list.tsx, src/lib/events/actions.ts</read_first>
  <action>
1. Create `components/admin/guest-list.tsx` — "use client" component (per D-24 through D-28):
   - Props: `{ guests: GuestRow[], stats: EventStats, eventId: string }`
   - **Stats header** (per D-28, ADMN-03): Four stat cards in a grid row:
     - "Regisztralt": `stats.totalRegistered` (with Users icon from lucide-react)
     - "Becsekkolva": `stats.checkedIn` (with CheckCircle icon)
     - "Nem jelent meg": `stats.noShows` (with XCircle icon)
     - "Becsekkolasi rata": `stats.checkInRate`% (with Percent icon)
     - Use shadcn Card components, compact styling matching existing admin UI
   - **Search input** (per D-26, ADMN-02): Text input at top of table
     - Client-side filtering: case-insensitive substring match on `name`, `firstName`, `lastName`, `email`
     - Placeholder: "Kereses nev vagy email alapjan..."
     - Filter state managed with useState, applied on every keystroke
   - **Guest table** (per D-25, ADMN-01): Columns:
     - Nev (firstName + lastName, or name as fallback)
     - Email
     - Regisztracio (formatted date via date-fns hu locale)
     - Statusz: show badge — green "Becsekkolva" if checkedInAt set, yellow "Regisztralt" if not, plus "INGYENES" badge if isFree
     - Becsekkolva (formatted checkedInAt datetime, or "—")
     - Muvelet: "Becsekkoltatás" button (per D-27) — only visible if NOT yet checked in
       - On click: call `manualCheckInByRegistrationId(registrationId, eventId)`
       - On success: update local state to reflect check-in (optimistic), show success toast or inline feedback
       - On error: show error message
   - Use `useTransition` for the manual check-in button pending state
   - Table rows alternate with subtle stripe: `even:bg-zinc-800/30`

2. Create `app/app/admin/events/[id]/guests/page.tsx` — Server Component (per D-24):
   - Follow pattern from `app/app/admin/events/[id]/edit/page.tsx` for dynamic params
   - Call `requireAdmin()`, redirect if not admin
   - Call `getEventById(params.id)` — notFound() if missing
   - Call `getEventGuests(params.id)` and `getEventStats(params.id)` in parallel
   - Render page header with event name + back link
   - Render `<GuestList guests={guests} stats={stats} eventId={id} />`
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Guest list page accessible at /app/admin/events/[id]/guests
    - Stats header shows 4 metrics: registered, checked-in, no-shows, check-in rate
    - Search input filters guests by name/email in real-time
    - Table shows all required columns (name, email, registration date, status, check-in time)
    - Manual check-in button works for non-checked-in guests
    - Free event registrations marked with "INGYENES" badge
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Guest list page with stats, search, table, and manual check-in buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Loyalty card, enhanced admin event list, and dashboard updates</name>
  <files>components/loyalty/loyalty-card.tsx, components/admin/event-list.tsx, app/app/page.tsx, app/app/admin/page.tsx, app/app/admin/events/page.tsx</files>
  <read_first>app/app/page.tsx, app/app/admin/page.tsx, app/app/admin/events/page.tsx, components/admin/event-list.tsx, src/lib/checkin/queries.ts</read_first>
  <action>
1. Create `components/loyalty/loyalty-card.tsx` — Server Component (per D-22, D-23, LOYL-01):
   - Props: `{ attendanceCount: number, nextIsFree: boolean }`
   - Display: 5 dots/circles in a row representing the 5-event cycle
     - Filled dots (amber/gold) for completed check-ins (0 to attendanceCount-1)
     - Empty dots (zinc-600) for remaining
     - The 5th dot has a special "INGYENES" star/gift icon
   - Below dots: text "X / 5 esemeny" showing progress
   - If `nextIsFree` (attendanceCount >= 4): show prominent amber label "Kovetkezo ingyenes!" (per D-22)
   - If attendanceCount === 0: show "Vegyel reszt esemenyeken es minden 5. alkalommal ingyenes belepot kapsz!"
   - Card styling: `bg-dozis-navy rounded-xl p-6 border border-zinc-800` matching existing dashboard cards
   - Title: "Husegprogram" with amber heading (per D-23)

2. Update `app/app/page.tsx` — Replace loyalty placeholder (per D-23):
   - Import `getUserLoyalty` from `src/lib/checkin/queries`
   - Call `getUserLoyalty(user.id)` alongside existing queries
   - Replace the "Hamarosan..." placeholder card with `<LoyaltyCard attendanceCount={loyalty.attendanceCount} nextIsFree={loyalty.nextIsFree} />`
   - Keep existing next event card and events link unchanged

3. Update `components/admin/event-list.tsx` — Add counts and quick links (per D-29, D-30):
   - Add new prop: `counts: Record<string, { registered: number, checkedIn: number }>` (serialized from Map)
   - For each event card, below the genre badges row, add a stats line:
     - "X regisztralt, Y becsekkolva" in muted text
   - Replace the existing action buttons area. New layout per event card (per D-30):
     - Three quick-link buttons: "Szkenner" (link to `/app/admin/events/${id}/scan`), "Vendeglista" (link to `/app/admin/events/${id}/guests`), "Szerkesztes" (existing edit link)
     - Keep "Torles" destructive button
     - Use lucide-react icons: ScanLine for scanner, Users for guests, Pencil for edit, Trash2 for delete
     - Buttons as small Link components or Buttons, arranged horizontally with gap-2

4. Update `app/app/admin/events/page.tsx` — Pass counts to EventList:
   - Import `getEventRegistrationCounts` from `src/lib/checkin/queries`
   - After fetching events, extract event IDs and call `getEventRegistrationCounts(eventIds)`
   - Convert Map to plain object for serialization (Server Component -> Client Component prop boundary)
   - Pass `counts` prop to `<EventList events={events} counts={countsObj} />`

5. Update `app/app/admin/page.tsx` — Add scanner quick link card:
   - Add a second navigation card below "Esemenyek kezelese":
     - Title: "Szkenner" or note that scanner is per-event (accessible from event list)
     - Or simply: keep existing card, the scanner/guest links are now per-event in the event list
   - Actually per D-30, the quick links are on event cards. The admin dashboard just needs the stats to be meaningful. Add a "Becsekkolva osszesen" stat card showing total check-ins across all active events. Query: sum of checkedIn from all active event stats. This is a nice-to-have — only add if straightforward.
   - Minimally: just ensure the admin dashboard links to the events page which now has the enhanced event list.
  </action>
  <verify>
    <automated>cd /Users/merlon/projects/DOZIS && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Loyalty card shows 5 dots with filled/empty state matching attendance count
    - "Kovetkezo ingyenes!" label shown when attendanceCount >= 4
    - User dashboard loyalty placeholder replaced with real loyalty card
    - Admin event list shows registration/check-in counts per event
    - Admin event cards have Szkenner, Vendeglista, Szerkesztes quick links
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Loyalty card on user dashboard, enhanced admin event list with counts and quick links, guest list page fully accessible from admin flow.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- User dashboard shows loyalty progress instead of placeholder
- Admin event list shows counts and quick links per event
- Guest list page accessible and shows stats + table + search
</verification>

<success_criteria>
- User sees X/5 loyalty progress with clear visual indicator
- Admin sees registered/checked-in counts on event list
- Admin can navigate to guest list from event card
- Guest list has search, stats, and manual check-in
- All pages work within existing admin auth protection
</success_criteria>

<output>
After completion, create `.planning/phases/03-checkin-loyalty/03-03-SUMMARY.md`
</output>
